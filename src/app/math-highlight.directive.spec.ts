import { MathHighlightDirective } from './math-highlight.directive';

describe('MathHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MathHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
